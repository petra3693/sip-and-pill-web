# Contact form delivery (server-side only — never exposed in the browser)
# Option A (recommended): free key from https://web3forms.com
WEB3FORMS_ACCESS_KEY=

# Option B: FormSubmit inbox (activates via confirmation email on first send)
CONTACT_TO_EMAIL=
# or
SUPPORT_EMAIL=
