# Contact form delivery (server-side only — never exposed in the browser)
# Free API key from https://resend.com — no domain verification needed as
# long as CONTACT_TO_EMAIL matches the email you signed up with.
RESEND_API_KEY=

# Inbox that receives contact form submissions.
CONTACT_TO_EMAIL=
# or
SUPPORT_EMAIL=
